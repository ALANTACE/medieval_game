#include <stdio.h>
#include <stdbit.h>

int main() {
    /* INITIALIZATIONS*/
    bool running = true;

    /* MAIN LOOP */
    while (running) {
        printf("Hello World!\n");
        running = false;
    }

    return 0;
}